import React from 'react';

const ParkLocItem = ({title, address, feature, distance}) =>{
    return(

    )
}

const ParkLocList = ({parkLocation, type}) =>{

    // let parkList;
    // if(type === 'cctv'){

    // }
    // else if(type === 'children'){

    // }
    // else{

    // }
    return(
        
    )
}

export default ParkLocList;